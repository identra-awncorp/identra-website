# Archived one-off scripts

This directory keeps historical repair, parsing, and test scripts that were used during localized content cleanup.

Current maintained scripts live directly in `scripts/` and are wired through `package.json`. Files in this archive are not part of the normal development workflow and should not be treated as maintained tooling unless they are intentionally promoted back into `scripts/`.
